<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/btn_calculator"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:backgroundTint="@color/color_dts"
        android:onClick="clickCalculator"
        android:text="Calculator" />

    <Button
        android:id="@+id/btn_explicit"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_below="@id/btn_calculator"
        android:backgroundTint="@color/color_dts"
        android:onClick="clickExplicit"
        android:text="Intent Explicit" />

    <Button
        android:id="@+id/btn_implicit"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_below="@id/btn_explicit"
        android:backgroundTint="@color/color_dts"
        android:onClick="clickImplisit"
        android:text="Intent Implicit" />

    <Button
        android:id="@+id/btn_listview"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_below="@id/btn_implicit"
        android:backgroundTint="@color/color_dts"
        android:onClick="clickListView"
        android:text="List View" />

    <Button
        android:id="@+id/btn_submit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:height="80dp"
        android:backgroundTint="@color/color_dts"
        android:onClick="clickSubmit"
        android:text="Submit" />
</RelativeLayout>